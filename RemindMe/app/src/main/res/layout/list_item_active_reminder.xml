<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="reminder"
            type="dev.shorthouse.remindme.model.Reminder" />
        <variable
            name="clickListener"
            type="dev.shorthouse.remindme.adapter.ActiveReminderListAdapter.ClickListener" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/active_reminder_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:onClick="@{() -> clickListener.onClick(reminder, activeReminderContainer.getId())}">


        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/reminder_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="16dp"
            android:maxLines="1"
            android:text="@{reminder.name}"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Headline6"
            app:layout_constraintEnd_toStartOf="@+id/reminder_done"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:ellipsize="end"
            tools:text="Brush teethBrush teethBrush teeth" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/elapsed_intervals"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            app:layout_constraintBottom_toBottomOf="@+id/reminder_date"
            app:layout_constraintStart_toEndOf="@+id/reminder_date"
            app:layout_constraintTop_toTopOf="@+id/reminder_date"
            app:reminderRepeatInterval="@{reminder.repeatInterval}"
            app:reminderStartDateTime="@{reminder.startDateTime}"
            tools:text="3" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/reminder_done"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/btn_reminder_done"
            android:onClick="@{() -> clickListener.onClick(reminder, reminderDone.getId())}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent">

        </com.google.android.material.button.MaterialButton>

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/reminder_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Body2"
            android:text="@{reminder.startDateTime.toLocalTime().toString()}"
            app:layout_constraintTop_toBottomOf="@id/reminder_name"
            app:layout_constraintStart_toStartOf="parent"
            tools:text="21:49" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/reminder_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Body2"
            app:layout_constraintStart_toEndOf="@+id/reminder_time"
            app:layout_constraintTop_toBottomOf="@+id/reminder_name"
            tools:text="2022-02-07" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="?attr/dividerHorizontal"
            android:layout_marginTop="16dp"
            app:layout_constraintTop_toBottomOf="@id/reminder_time"
            app:layout_constraintStart_toStartOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>